# fuck_your_lan
Creates a fake "Evil Twin" WiFi AP with the exact same SSID as the target network Broadcasts on the same channel with maximum power Runs a rogue DHCP server to hand out IPs Hosts a phishing captive portal that looks like the real router login page (steals credentials) Forces all clients to deauth on the legit AP MITM all traffi
